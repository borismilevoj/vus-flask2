services:
  - type: web
    name: vus-flask2
    env: python
    plan: free
    rootDir: .
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python prestart.py && gunicorn -b 0.0.0.0:$PORT app:app"


